"use server"
import React from 'react';
import LoginPage from "@/modules/auth/components/features/LoginPage";

const page = async () => {
    return (
        <LoginPage/>
    );
};

export default page;